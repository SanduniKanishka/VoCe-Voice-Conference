Place this in two machines

Compile Client.java

Then all classes must be compiled. If not each class must be compiled.

Run Client with IP address as a command line argument.
ex :- java Client 127.0.0.1